import React__default, { forwardRef } from 'react';
import '@remixicon/react';
import { Icon } from '../Icon/Icon.esm.js';
import '../Icon/context.esm.js';
import clsx from 'clsx';
import { useResponsiveValue } from '../../hooks/useResponsiveValue.esm.js';

const Button = forwardRef(
  (props, ref) => {
    const {
      size = "medium",
      variant = "primary",
      disabled,
      iconStart,
      iconEnd,
      children,
      className,
      style,
      ...rest
    } = props;
    const responsiveSize = useResponsiveValue(size);
    const responsiveVariant = useResponsiveValue(variant);
    return /* @__PURE__ */ React__default.createElement(
      "button",
      {
        ref,
        disabled,
        className: clsx(
          "canon-Button",
          `canon-Button--size-${responsiveSize}`,
          `canon-Button--variant-${responsiveVariant}`,
          className
        ),
        style,
        ...rest
      },
      iconStart && /* @__PURE__ */ React__default.createElement(Icon, { name: iconStart, className: "canon-Button--icon" }),
      children,
      iconEnd && /* @__PURE__ */ React__default.createElement(Icon, { name: iconEnd, className: "canon-Button--icon" })
    );
  }
);

export { Button, Button as default };
//# sourceMappingURL=Button.esm.js.map
