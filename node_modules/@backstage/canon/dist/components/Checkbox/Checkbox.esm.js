import React__default from 'react';
import { Checkbox as Checkbox$1 } from '@base-ui-components/react/checkbox';
import { Icon } from '@backstage/canon';
import clsx from 'clsx';

const Checkbox = React__default.forwardRef(
  (props, ref) => {
    const {
      label,
      checked,
      onChange,
      disabled,
      required,
      className,
      name,
      value,
      style
    } = props;
    const checkboxElement = /* @__PURE__ */ React__default.createElement(
      Checkbox$1.Root,
      {
        ref,
        className: clsx("canon-CheckboxRoot", className),
        checked,
        onCheckedChange: onChange,
        disabled,
        required,
        name,
        value,
        style
      },
      /* @__PURE__ */ React__default.createElement(Checkbox$1.Indicator, { className: "canon-CheckboxIndicator" }, /* @__PURE__ */ React__default.createElement(Icon, { name: "check", size: 12 }))
    );
    return label ? /* @__PURE__ */ React__default.createElement("label", { className: "canon-CheckboxLabel" }, checkboxElement, label) : checkboxElement;
  }
);

export { Checkbox };
//# sourceMappingURL=Checkbox.esm.js.map
