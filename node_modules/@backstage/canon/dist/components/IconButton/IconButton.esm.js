import React__default, { forwardRef } from 'react';
import '@remixicon/react';
import { Icon } from '../Icon/Icon.esm.js';
import '../Icon/context.esm.js';
import clsx from 'clsx';
import { useResponsiveValue } from '../../hooks/useResponsiveValue.esm.js';

const IconButton = forwardRef(
  (props, ref) => {
    const {
      size = "medium",
      variant = "primary",
      icon,
      className,
      style,
      ...rest
    } = props;
    const responsiveSize = useResponsiveValue(size);
    const responsiveVariant = useResponsiveValue(variant);
    return /* @__PURE__ */ React__default.createElement(
      "button",
      {
        ref,
        className: clsx(
          "canon-IconButton",
          `canon-IconButton--size-${responsiveSize}`,
          `canon-IconButton--variant-${responsiveVariant}`,
          className
        ),
        style,
        ...rest
      },
      /* @__PURE__ */ React__default.createElement(Icon, { name: icon, className: "canon-IconButton--icon" })
    );
  }
);

export { IconButton, IconButton as default };
//# sourceMappingURL=IconButton.esm.js.map
